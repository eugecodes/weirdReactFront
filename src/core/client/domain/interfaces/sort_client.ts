import type { Order } from "@/src/core/app/domain/models/order";

export interface SortClient {
  id?: Order;
  alias?: Order;
  fiscalName?: Order;
  clientName?: Order;
  clientType?: Order;
  cif?: Order;
  createdAt?: Order;
  user?: Order;
}
