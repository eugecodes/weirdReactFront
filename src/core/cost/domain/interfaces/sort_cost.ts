import type { Order } from "@/src/core/app/domain/models/order";
export interface SortCost {
  name?: Order;
  clientTypes?: Order;
  rates?: Order;
  minPower?: Order;
  maxPower?: Order;
  quantity?: Order;
  costType?: Order;
  enabled?: Order;
  createdAt?: Order;
  responsible?: Order;
}
