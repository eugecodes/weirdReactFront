import type { Order } from "@/src/core/app/domain/models/order";

export interface SortContact {
  id?: Order;
  name?: Order;
  email?: Order;
  phone?: Order;
  createdAt?: Order;
  user?: Order;
}
