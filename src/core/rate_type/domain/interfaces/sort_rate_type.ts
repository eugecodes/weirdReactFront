import type { Order } from "@/src/core/app/domain/models/order";

export interface SortRateType {
  name?: Order;
  energyType?: Order;
  minPower?: Order;
  maxPower?: Order;
  enabled?: Order;
  createdAt?: Order;
  responsible?: Order;
}
