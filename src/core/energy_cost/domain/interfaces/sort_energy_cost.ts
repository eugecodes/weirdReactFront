import type { Order } from "@/src/core/app/domain/models/order";
export interface SortEnergyCost {
  concept?: Order;
  amount?: Order;
  enabled?: Order;
  createdAt?: Order;
  responsible?: Order;
}
