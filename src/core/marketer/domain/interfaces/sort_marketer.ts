import type { Order } from "@/src/core/app/domain/models/order";

export interface SortMarketer {
  name?: Order;
  fiscalName?: Order;
  cif?: Order;
  email?: Order;
  enabled?: Order;
  createdAt?: Order;
  responsible?: Order;
}
