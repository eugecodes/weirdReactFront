import type { Order } from "@/src/core/app/domain/models/order";

export interface SortMarketerMargin {
  rate?: Order;
  type?: Order;
  rateType?: Order;
  minConsume?: Order;
  maxConsume?: Order;
  minMargin?: Order;
  maxMargin?: Order;
  createdAt?: Order;
  responsible?: Order;
}
